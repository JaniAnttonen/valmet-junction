import React from 'react'
import { Pane, Text, Heading } from 'evergreen-ui'

const Current = props => {
  return <Pane display="flex" width="50%" padding={25}><Heading size="600">Current Status</Heading></Pane>
}

export default Current
